{
  "private": true,
  "type": "module",
  "scripts": {
    "lite-install": "npm install --omit=dev --ignore-scripts",
    "build": "npm run build --prefix shared && npm run build --prefix client && npm run build --prefix server",
    "start": "NODE_ENV=production node server/dist/index.js",
    "lite-build": "./lite-build.sh"
  },
  "workspaces": [
    "server",
    "client",
    "shared"
  ]
}
